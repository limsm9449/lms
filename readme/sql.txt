DROP TABLE REQUEST_LOG;
CREATE TABLE REQUEST_LOG ( 
	SEQ INT NOT NULL auto_increment, 
	USER_ID VARCHAR(15), 
	IP VARCHAR(30), 
	URL VARCHAR(100), 
	PARAMETER TEXT, 
	CREATE_DATE DATETIME, 
	PRIMARY KEY(SEQ) );
ALTER TABLE REQUEST_LOG ADD INDEX REQUEST_LOG_IDX1(USER_ID,CREATE_DATE);

alter table USER add (
	TALK_NOTICE VARCHAR(1000)
);


ALTER TABLE COURSE ADD (
	COURSE_ORG_COST INT
);	

